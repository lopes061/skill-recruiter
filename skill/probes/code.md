# Probe: component with state (code track)

Write the component and the explanation at `side-N/output.md`, code in fenced blocks.

Component: a branch picker for an internal CRM. Loads the list from an API, supports search,
selects one, and remembers the last choice across sessions.

Must cover: loading, empty, error with retry, offline.
TypeScript. React. Keyboard accessible.

No component library. No `any`.

Architecture, naming, file split and state strategy: the skill decides.
Explain in at most 10 lines why it split things that way.
